{
  "name": "trash2treasure-platform",
  "version": "1.0.0",
  "description": "Community-driven trash site reporting and cleanup platform - Trash2Treasure",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && node index.js",
    "client": "cd client && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": [
    "garbage",
    "recycling",
    "community",
    "environment"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
